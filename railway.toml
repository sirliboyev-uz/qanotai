[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn app.main_complete:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 30

[variables]
PYTHON_VERSION = "3.11"