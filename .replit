run = "python -m uvicorn app.main_complete:app --host 0.0.0.0 --port 8000"
language = "python3"

[deployment]
run = ["sh", "-c", "python -m uvicorn app.main_complete:app --host 0.0.0.0 --port 8000"]

[env]
PYTHONPATH = "/home/runner/${REPL_SLUG}"

[[ports]]
localPort = 8000
externalPort = 80